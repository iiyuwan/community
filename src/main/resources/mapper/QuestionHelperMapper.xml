<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.juice.community.mapper.QuestionHelperMapper">
  <resultMap id="BaseResultMap" type="com.juice.community.model.Question">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Thu Feb 13 17:34:13 CST 2020.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="title" jdbcType="VARCHAR" property="title" />
    <result column="description" jdbcType="VARCHAR" property="description" />
    <result column="gmt_create" jdbcType="BIGINT" property="gmt_create" />
    <result column="gmt_modified" jdbcType="BIGINT" property="gmt_modified" />
    <result column="creator" jdbcType="INTEGER" property="creator" />
    <result column="comment_count" jdbcType="INTEGER" property="comment_count" />
    <result column="review_count" jdbcType="INTEGER" property="review_count" />
    <result column="like_count" jdbcType="INTEGER" property="like_count" />
    <result column="tag" jdbcType="VARCHAR" property="tag" />
  </resultMap>
  <update id="addReviewCount" parameterType="com.juice.community.model.Question">
    update question
    set  review_count =review_count+ #{review_count,jdbcType=INTEGER}
    where id=#{id}
  </update>
  <update id="addCommentCount" parameterType="com.juice.community.model.Question">
    update question
    set   comment_count=comment_count+ #{comment_count,jdbcType=INTEGER}
    where id=#{id}
  </update>
  <!--查询相关推荐问题 主要是tag含有相同的--> <!--parameterType是传入的参数类型-->
  <select id="selectRelatedQuestion" parameterType="com.juice.community.model.Question" resultMap="BaseResultMap">
    select * from question where id!=#{id} and tag regexp #{tag}
  </select>
  <select id="countByKeyWords" parameterType="com.juice.community.dto.QuestionQueryDTO" resultType="java.lang.Integer">
       select count(*) from question
    <where>
       <if test="keyStr!=null">
          and title regexp #{keyStr}
       </if>
    </where>
  </select>

  <select id="selectByKeyWords" parameterType="com.juice.community.dto.QuestionQueryDTO" resultMap="BaseResultMap">
    select * from question
    <where>
      <if test="keyStr!=null">
        and title regexp #{keyStr}
      </if>
    </where>
    order by gmt_create desc limit #{page},#{size}
  </select>

</mapper>